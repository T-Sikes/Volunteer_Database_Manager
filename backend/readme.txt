To activate backend you must run a server using
python manage.py runserver (Windows users it is reccomended you activate a python virtual environment)

For Windows Users:
python -m venv venv - will create a folder with needed dependencies
venv\Scripts\activate - activate virtual environment. 

Backend uses Django, to install django run 
pip install -r requirements.txt


